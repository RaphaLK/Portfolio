export const metadata = {
  title: "Projects",
  description:
    "Technical projects across systems programming, compiler design, signal processing, and full-stack applications.",
};

export default function ProjectsLayout({ children }) {
  return children;
}
